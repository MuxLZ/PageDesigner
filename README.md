# 医院康养官网模板 - 可视化页面设计系统

一个基于 Vue 3 + TypeScript 的低代码页面设计系统，通过模块化架构实现页面的可视化配置和快速搭建。适用于医院、康养机构等官网建设。

## ✨ 功能特性

- 🎨 **可视化设计器** - 拖拽式页面设计，无需编写代码即可搭建专业网站
- 📦 **丰富的模块库** - 内置多种模块类型（导航、轮播、统计、时间轴等）
- 🎭 **动画效果系统** - 支持多种动画效果，提升页面交互体验
- 🎨 **主题定制** - 支持自定义主题颜色、字体、间距等样式配置
- 📱 **响应式设计** - 自动适配不同屏幕尺寸
- 💾 **配置导出/导入** - 支持配置的保存、导出和复用
- 👁️ **实时预览** - 设计过程中实时预览页面效果
- 🔧 **模块化架构** - 易于扩展和维护的模块化设计

## 🛠️ 技术栈

- **框架**: Vue 3.3.4 + TypeScript 5.1.6
- **构建工具**: Vite 4.4.7
- **UI 组件库**: Element Plus 2.3.8
- **状态管理**: Pinia 2.1.6
- **路由**: Vue Router 4.2.4
- **拖拽排序**: SortableJS 1.15.6
- **动画库**: AOS 2.3.4
- **轮播组件**: Swiper 10.0.4
- **工具库**: VueUse 14.0.0

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 `http://localhost:8091` 查看应用

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 📁 项目结构

```
pageVue1/
├── src/
│   ├── assets/          # 静态资源
│   │   └── styles/       # 全局样式文件
│   ├── components/       # 组件
│   │   ├── Layout/       # 布局组件
│   │   └── modules/       # 模块组件库
│   ├── core/             # 核心功能
│   │   ├── BaseModule.ts          # 模块基类
│   │   ├── ConfigGenerator.ts     # 配置生成器
│   │   ├── EffectSystem.ts         # 效果系统
│   │   ├── ModuleRegistry.ts       # 模块注册
│   │   ├── PageRenderer.vue        # 页面渲染器
│   │   └── StyleSystem.ts          # 样式系统
│   ├── router/           # 路由配置
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   └── views/            # 页面视图
│       ├── Designer.vue  # 设计器界面
│       ├── Preview.vue   # 预览界面
│       └── ...
├── docs/                 # 文档
│   ├── ARCHITECTURE.md           # 架构文档
│   ├── MODULE_EXTENSION_GUIDE.md # 模块扩展指南
│   └── PREVIEW_GUIDE.md          # 预览指南
├── DEPLOY.md             # 部署文档
└── README.md             # 项目说明
```

## 📖 使用指南

### 访问设计器

在浏览器中访问 `/designer` 路径，进入可视化设计器界面。

### 添加模块

1. 在左侧模块库中选择要添加的模块
2. 点击模块卡片，模块会自动添加到页面中
3. 在右侧配置面板中编辑模块属性

### 配置模块

每个模块都支持以下配置：

- **基础配置**: 名称、可见性、排序等
- **样式配置**: 背景色、内边距、字体等
- **动画效果**: 淡入、滑入、缩放等动画效果
- **模块特定配置**: 根据模块类型的不同配置项

### 导出配置

1. 在设计器中完成页面设计
2. 点击工具栏的"导出配置"按钮
3. 下载 JSON 格式的配置文件

### 预览页面

1. 点击工具栏的"导出并预览"按钮
2. 或访问 `/preview` 路径
3. 上传配置文件查看页面效果

更多使用说明请查看 [预览指南](./docs/PREVIEW_GUIDE.md)

## 🎨 内置模块

### 布局模块
- **HeaderModule** - 导航栏模块
- **FooterModule** - 页脚模块
- **ColumnModule** - 分栏模块
- **GridModule** - 宫格模块

### 内容模块
- **PageHeroModule** - 页面英雄区模块
- **TextImageModule** - 图文混排模块
- **CarouselModule** - 轮播图模块
- **CardListModule** - 卡片列表模块

### 业务模块
- **StatsModule** - 统计数据模块
- **TimelineModule** - 时间轴模块
- **ProductsModule** - 产品展示模块
- **SolutionsModule** - 解决方案模块
- **NewsModule** - 新闻资讯模块
- **TeamModule** - 团队介绍模块
- **PartnersModule** - 合作伙伴模块
- **HonorsModule** - 荣誉资质模块

### 交互模块
- **FeatureListModule** - 功能列表模块
- **CTAModule** - 行动号召模块
- **ContactFormModule** - 联系表单模块

## 🔧 开发指南

### 扩展新模块

1. 在 `src/types/module.ts` 中定义模块类型和配置接口
2. 在 `src/components/modules/` 中创建模块组件
3. 在 `src/core/PageRenderer.vue` 中注册模块组件
4. 在 `src/core/ConfigGenerator.ts` 中添加默认配置

详细步骤请查看 [模块扩展指南](./docs/MODULE_EXTENSION_GUIDE.md)

### 架构说明

系统采用分层架构设计：

- **视图层**: Designer.vue - 可视化设计界面
- **渲染层**: PageRenderer.vue - 页面渲染引擎
- **模块层**: 各种模块组件
- **支撑层**: 样式系统、效果系统、配置生成器等

详细架构说明请查看 [架构文档](./docs/ARCHITECTURE.md)

## 🚀 部署

### Linux 服务器部署

项目提供了完整的部署脚本，支持快速部署到 Linux 服务器。

#### 快速启动

```bash
chmod +x quick-start.sh
./quick-start.sh
```

#### 使用管理脚本

```bash
chmod +x start.sh
./start.sh start    # 启动服务
./start.sh stop     # 停止服务
./start.sh restart  # 重启服务
./start.sh status   # 查看状态
./start.sh logs     # 查看日志
```

#### 自定义端口

```bash
PORT=8080 ./start.sh start
```

详细部署说明请查看 [部署文档](./DEPLOY.md)

### 使用 PM2 管理（推荐生产环境）

```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start npm --name "vue-website" -- run preview -- --host 0.0.0.0 --port 8091

# 查看状态
pm2 status

# 查看日志
pm2 logs vue-website

# 停止服务
pm2 stop vue-website

# 开机自启
pm2 startup
pm2 save
```

### 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8091;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## 📝 脚本说明

- `npm run dev` - 启动开发服务器
- `npm run build` - 构建生产版本
- `npm run build:check` - 类型检查 + 构建
- `npm run type-check` - TypeScript 类型检查
- `npm run preview` - 预览生产构建

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

本项目采用 MIT 许可证。

## 📞 联系方式

如有问题或建议，请提交 Issue。

---

**注意**: 这是一个低代码页面设计系统，适合快速搭建官网。如需更多定制功能，请参考开发指南进行扩展。

